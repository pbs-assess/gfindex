---
title: "Trawl survey index standardization for `r paste(params$species)`"
author: "Philina English"
date: "`r Sys.Date()`"
output: html_document
params:
    species: "Canary Rockfish"
    # region: "SYN WCHG"
    # region: "SYN QCS"
    region: "SYN HS"
    # region: "SYN WCVI"
    # survey: "Coast-wide trawl surveys"
    # covariate_type: "no depth" # or "with depth
    covariate_type: "with depth"
    # update_model: FALSE
    update_model: TRUE # will over write past models; will always attempt a model if none exists
    update_index: TRUE
    share_range: TRUE
    anisotropy: FALSE
    qq_residuals: FALSE
    silent: TRUE
---

```{r global_options, include=FALSE}
knitr::opts_chunk$set(
  fig.width = 11, fig.height = 8.5,
  echo = FALSE, warning = FALSE, message = FALSE
)

library(dplyr)
library(ggplot2)
library(sdmTMB)
library(here)

# options(scipen = 999)
theme_set(
  theme_bw(base_size = 14)
)

dir.create(here("predictions"), showWarnings = FALSE)
dir.create(here("indices"), showWarnings = FALSE)
dir.create(here("figs"), showWarnings = FALSE)
```

```{r params}
species <- params$species
region <- params$region
covariate_type <- gsub(" ", "-", gsub("\\/", "-", tolower(params$covariate_type)))
update_model <- params$update_model
update_index <- params$update_index
plot_index <- TRUE
# paste("survey =", region)
# paste("model type =", params$covariate_type)
print(params)
family <- delta_gamma()

if (region == "Coast-wide trawl surveys") {
  region <- c("SYN QCS", "SYN HS", "SYN WCVI", "SYN WCHG")
}
```

Get species data 

```{r dat}
spp <- gsub(" ", "-", gsub("\\/", "-", tolower(species)))

data <- readRDS(here("data/all-survey-sets-2021.rds")) %>%
  rename(survey_series_id = survey_series_id.x) %>%
  filter(species_common_name == tolower(species)) %>%
  filter(depth_m > 0)

# change from per m2 to per km2:
data$density <- data[["density_kgpm2"]] * 1000000
data$log_depth <- log(data$depth_m)

positive_sets <- data %>%
  filter(density != 0) %>%
  filter(survey_abbrev %in% region)
data <- data %>% filter(survey_abbrev %in% unique(positive_sets$survey_abbrev))
ssid_string <- paste0(unique(data$survey_series_id), collapse = "n")
# surv_string <- paste0(unique(data$survey_abbrev), collapse = ", ")
data <- sdmTMB::add_utm_columns(data, c("longitude", "latitude"), utm_crs = 32609)
```

<!-- Make mesh -->

```{r mesh}
# standard settings
cutoff <- 10
spatiotemporal <- "iid"

if (region %in% c("SYN WCHG")) {
  cutoff <- 7
}
spde <- make_mesh(data, xy_cols = c("X", "Y"), cutoff = cutoff)
plot(spde)
spde$mesh$n
```

```{r load-cached}
# Check if a model already exists
dir.create("models", showWarnings = FALSE)
f <- here(paste0("models/m-", spp, "-", ssid_string, "-", covariate_type, ".rds"))
if (file.exists(f)) {
  m <- readRDS(f)
} else {
  update_model <- TRUE
}
```

<!-- Run sdmTMB model -->

```{r fit-models}
if (update_model) {
  update_index <- TRUE

  if (covariate_type == "no-depth") {
    formula <- density ~ 0 + as.factor(year)
  } else {
    if (covariate_type == "with-depth") {
      formula <- density ~ 0 + as.factor(year) + poly(log_depth, 2)
    }
  }

  set_priors <- sdmTMBpriors(
    matern_s = pc_matern(range_gt = 5, sigma_lt = 3),
    matern_st = pc_matern(range_gt = 5, sigma_lt = 3)
  )
  ctrl <- sdmTMBcontrol(newton_loops = 1L)

  m <- sdmTMB(
    formula,
    data,
    mesh = spde,
    time = "year",
    family = family,
    spatial = "on",
    spatiotemporal = "iid",
    share_range = params$share_range,
    anisotropy = params$anisotropy,
    silent = params$silent,
    control = ctrl
  )

  s <- sanity(m)

  if (!s$all_ok && isTRUE(family$delta)) {
    # turn off spatiotemporal fields for binomial
    m <- sdmTMB(
      formula,
      data,
      mesh = spde,
      time = "year",
      family = family,
      spatial = list("on", "on"),
      spatiotemporal = list("off", "iid"),
      share_range = params$share_range,
      anisotropy = params$anisotropy,
      silent = params$silent,
      control = ctrl
    )
    s <- sanity(m)
  }

  if (!s$all_ok && isTRUE(family$delta)) {
    # try tweedie
    m <- sdmTMB(
      formula,
      data,
      mesh = spde,
      time = "year",
      family = tweedie(),
      spatial = "on",
      spatiotemporal = "iid",
      share_range = params$share_range,
      anisotropy = params$anisotropy,
      silent = params$silent,
      control = ctrl
    )
    s <- sanity(m)
  }
  
  if (!s$all_ok && isTRUE(family$delta)) {
    # try tweedie without spatiotemporal
    m <- sdmTMB(
      formula,
      data,
      mesh = spde,
      time = "year",
      family = tweedie(),
      spatial = "on",
      spatiotemporal = "off",
      share_range = params$share_range,
      anisotropy = params$anisotropy,
      silent = params$silent,
      control = ctrl
    )
    s <- sanity(m)
  }

  saveRDS(m, file = here(paste0("models/m-", spp, "-", ssid_string, "-", covariate_type, ".rds")))
}
```


```{r read-model, warning = TRUE, message = TRUE}
m <- readRDS(here(paste0("models/m-", spp, "-", ssid_string, "-", covariate_type, ".rds")))
m

s <- sanity(m)

tidy(m, effects = "ran_pars", conf.int = TRUE)
if (length(m$call$family) > 1L) tidy(m, effects = "ran_pars", conf.int = TRUE, model = 2L)
```

```{r plot-aniso}
if (isTRUE(params$anisotropy)) {
  sdmTMB:::plot_anisotropy2(m)
}
```

```{r residuals, warning = FALSE, message = FALSE, eval=params$qq_residuals}
if (!s$all_ok) {
  plot_index <- FALSE
  update_index <- FALSE
  print("Model still not converging.")
} else {
  r <- residuals(m, "mle-mcmc", mcmc_warmup = 100, mcmc_iter = 101, model = 1L)
  r2 <- r != Inf
  if (!all(r2)) {
    r3 <- r[r2]
    qqnorm(r3)
    qqline(r3)
    update_index <- FALSE
    plot_index <- FALSE
    print(" Some residuals are infinite, so not calculating index.")
  } else {
    qqnorm(r, main = "Model 1 QQ plot")
    qqline(r)
  }
  if (length(m$family$family) > 1L) {
    r <- residuals(m, "mle-mcmc", mcmc_warmup = 100, mcmc_iter = 101, model = 2L)
    qqnorm(r, main = "Model 2 QQ plot")
    qqline(r)
  }
}
```

<!-- Load grid -->

```{r load-grid, message=FALSE, warning=FALSE}
f <- here(paste0("indices/i-", spp, "-", ssid_string, "-", covariate_type, ".rds"))
if (!file.exists(f)) {
  update_index <- TRUE
}

nd <- readRDS(here("grids/synoptic_grid.rds")) %>%
  filter(survey == region)

if (update_index) {
  if (region == "SYN QCS") {
    model_ssid <- c(1)
  }

  if (region == "SYN HS") {
    model_ssid <- c(3)
  }

  if (region == "SYN WCVI") {
    model_ssid <- c(4)
  }

  if (region == "SYN WCHG") {
    model_ssid <- c(16)
  }

  if (region == "Coast-wide trawl surveys") {
    nd <- readRDS(here("data-generated/nd_whole_coast_index.rds")) %>% mutate(cell_area = 4)
  }
}
```

<!-- Get index -->

```{r get-index}
if (update_index) {
  make_grid <- function(x, years) {
    .x <- x
    years <- sort(unique(years))
    .nd <- do.call(
      "rbind",
      replicate(length(years), .x, simplify = FALSE)
    )
    .nd$year <- rep(years, each = nrow(.x))
    .nd
  }

  fitted_yrs <- sort(unique(m$data$year))

  nd <- make_grid(nd, years = fitted_yrs)
  nd <- na.omit(nd)
  nd$year <- as.integer(nd$year)
  nd$log_depth <- log(nd$depth)

  p <- predict(m, newdata = nd, return_tmb_object = TRUE)

  saveRDS(p$data, here(paste0("predictions/p-", spp, "-", ssid_string, "-", covariate_type, ".rds")))

  i <- get_index(p, area = nd$cell_area, bias_correct = TRUE)

  i <- i %>% mutate(
    species = species,
    survey = region,
    surveys = paste(unique(positive_sets$survey_abbrev), collapse = ", "),
    ssids = paste(unique(positive_sets$survey_series_id), collapse = ", "),
    ssid_string = ssid_string,
    family = ifelse(length(m$family$family) > 1, m$family$clean_name, m$family$family),
    anisotropy = params$anisotropy,
    spatiotemporal = paste(c(m$call$spatiotemporal), collapse = " "),
    share_range = params$share_range,
    model = covariate_type,
    max_grad = max(m$gradients)
  )
  saveRDS(i, here(paste0("indices/i-", spp, "-", ssid_string, "-", covariate_type, ".rds")))
}
```

Plot index
```{r plot-index}
f <- here(paste0("indices/i-", spp, "-", ssid_string, "-", covariate_type, ".rds"))
if (file.exists(f)) {
  if (plot_index) {
    i <- readRDS(f)
    g <- ggplot(i, aes(year, est / 1000)) +
      geom_line() +
      geom_ribbon(aes(ymin = lwr / 1000, ymax = upr / 1000), alpha = 0.4) +
      ylab("Biomass (tonnes)") +
      ggtitle(paste(species), subtitle = paste0(region))
    ggsave(here(paste0("figs/", spp, "-", ssid_string, covariate_type, ".png")), height = 4, width = 6)
    g
  }
}
```

<!-- Map predictions -->

```{r plot-predictions}
f <- here(paste0("predictions/p-", spp, "-", ssid_string, "-", covariate_type, ".rds"))
if (plot_index) {
  
  plot_map <- function(dat, column) {
    ggplot(dat, aes_string("X", "Y", fill = column)) +
      geom_tile() +
      scale_colour_viridis_c() +
      scale_fill_viridis_c() +
      coord_fixed()
  }
  
  if (file.exists(f)) {
    p2 <- readRDS(f)
    if (length(m$family$family) == 1L) {
      plot_map(p2, "est") +
        facet_wrap(~year) +
        ggtitle("Fixed + random effects in link space")
    } else {
      plot_map(p2, "est1") +
        facet_wrap(~year) +
        ggtitle("Model 1 fixed + random effects in link space")
    }
  }
}
```

```{r plot-index2}
if (plot_index) {
  if (file.exists(f)) {
    if (length(m$family$family) > 1L) {
      plot_map(p2, "est2") +
        facet_wrap(~year) +
        ggtitle("Model 2 fixed + random effects in link space")
    }
  }
}
```


```{r plot-biomass}
if (plot_index) {
  p <- predict(m, newdata = nd, type = "response")
  
  plot_map(p, "est") +
    facet_wrap(~year) +
    scale_fill_viridis_c(trans = "sqrt", na.value = "yellow", limits = c(0, quantile(p$est, 0.975))) +
    ggtitle("Biomass estimates")
}
```
